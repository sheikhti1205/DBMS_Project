desktop.ini
.DS_Store
__pycache__/
*.pyc
.venv/
*.building
*.restoring
*.syncing

# The original datasets live in Google Drive, not GitHub.
/Selected_Source_Files/
/source/
/sources/
/raw_data/

# LaTeX build files
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lol
*.lot
*.out
*.synctex.gz
*.toc
*.xdv

# Generated report measurements are committed under report/scripts/generated/.

# Local tooling
/.agents/
/.serena/
/graphify-out/
/outputs/
/tmp/
/work_scripts/
/schema/backups/
tempCodeRunnerFile.*

# Local-only convenience tools (Drive publishing, interactive demo); not part
# of the delivered evidence or the GitHub source of truth.
/schema/scripts/maintenance/publish_to_drive.py
/schema/scripts/queries/demo_database.py

# Local symlink to the shared Drive folder that holds the original workbooks.
# The normalization pipeline reads through it; the target path is per-machine,
# so the link itself must not be committed.
/Selected_Source_Files

# Keep the coding repository free of report copies and old working versions
/deliverables/
/report_v*/
/report_versions/
/work/
/work_v*/

# Compiled report output (source and generated evidence are committed)
/report/main.pdf
/*.zip

# Internal process notes and review documents are kept locally, never pushed.
/_internal/
